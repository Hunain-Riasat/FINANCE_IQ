# ══════════════════════════════════════════════════════
#  AI Financial Intelligence Platform — Requirements
#  Original deps preserved + new enhanced features
# ══════════════════════════════════════════════════════

# ── Core / Original ──────────────────────────────────
streamlit>=1.28.1
pandas>=2.0.3
numpy>=1.24.3
plotly>=5.16.1
scikit-learn>=1.3.1
scipy>=1.11.2
openpyxl>=3.1.5
reportlab>=4.0.7
PyPDF2>=3.0.1
python-dateutil>=2.8.2
statsmodels>=0.14.0

# ── AI / LLM APIs ────────────────────────────────────
anthropic>=0.25.0          # Claude API (primary chatbot)
# google-generativeai>=0.5.0 # Gemini API (optional fallback) — uncomment if needed

# ── PDF Parsing ──────────────────────────────────────
pdfplumber>=0.10.0         # PDF table extraction
tabula-py>=2.9.0           # Alternative PDF table parser (needs Java)
# pdfminer.six             # Additional PDF text extraction (optional)

# ── OCR Support ──────────────────────────────────────
Pillow>=10.0.0             # Image handling
pytesseract>=0.3.10        # Tesseract OCR wrapper (requires Tesseract installed)
# Note: Install Tesseract binary separately:
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki
#   Ubuntu:  sudo apt install tesseract-ocr
#   macOS:   brew install tesseract

# ── ML / Forecasting ─────────────────────────────────
# prophet>=1.1.5            # Facebook Prophet (optional; heavy install)
# lightgbm>=4.0.0           # Gradient boosting for predictions (optional)

# ── Data Generation ──────────────────────────────────
faker>=20.0.0              # Synthetic data generation

# ── Utilities ────────────────────────────────────────
python-dotenv>=1.0.0       # .env file support for API keys
requests>=2.31.0           # HTTP requests

# ══════════════════════════════════════════════════════
#  INSTALL:
#    pip install -r requirements.txt
#
#  API KEYS (set as environment variables):
#    ANTHROPIC_API_KEY=sk-ant-...   ← required for AI chat
#    GEMINI_API_KEY=AIza...         ← optional fallback
#
#  RUN:
#    streamlit run app_enhanced.py
# ══════════════════════════════════════════════════════
